// ternary operator
let age = 20;
let result = age > 5 ? "you are" : "your not";
console.log(result);

let score = 60;
let outCome =
  score > 50
    ? "go baby"
    : score > 80
    ? "you are wrong"
    : score > 40
    ? "your go"
    : "worse";

console.log(outCome);
